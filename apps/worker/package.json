{
  "name": "worker",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/index.ts",
    "start": "node --env-file-if-exists=.env dist/index.js",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@carelog/ai": "workspace:*",
    "@carelog/db": "workspace:*",
    "@carelog/queue": "workspace:*",
    "@carelog/storage": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.23.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.9"
  }
}
